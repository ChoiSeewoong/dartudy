import 'package:flutter/widgets.dart';

class DartPadEmbed extends StatelessWidget {
  const DartPadEmbed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('⚠️ 이 기능은 웹에서만 사용할 수 있습니다.'));
  }
}
